services:
  import-sqlite:
    image: ${DISCVAULT_NEXT_IMAGE}
    container_name: discvault-next-import-sqlite
    restart: "no"
    working_dir: /opt/discvault/backend
    command:
      [
        "python",
        "next_import.py",
        "--db",
        "/data/discvault.db",
        "--data-dir",
        "/data"
      ]
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      TZ: ${TZ}
    volumes:
      - ${DISCVAULT_DATA_DIR}:/data:ro
    networks:
      - discvault-next

networks:
  discvault-next:
    external: true
    name: discvault-next
