# Container Schema

DiscVault supports vaults, box sets, and collections.

The detailed normalized container schema, migration notes, compatibility rules,
and client payload contracts are intentionally not published in this repository.
Treat the storage model as an internal implementation detail.

Use the app's public UI, documented API summaries, and backup/restore features
instead of depending on internal table names or migration behavior.
